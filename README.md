# AOE
Generate models(Go struct) from PostgreSQL tables.
The name `aoe` doesn't mean anything. It's just the first three letters on the left hand home row of the Dvorak layout.

**It works for me but it won't work for most of people.**
